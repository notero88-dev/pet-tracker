# Status — 2026-04-22

## Shipped

- ✅ **Codemagic iOS build pipeline** — `ios-simulator-debug` workflow green on `main`. `ios-testflight` wired but not yet exercised (pending Apple Developer validation).
- ✅ **Flutter web dev loop** — `flutter run -d chrome --web-port=8080` confirmed working end-to-end. Used for the first real user-flow test.
- ✅ **`mobile_scanner` upgraded** — `6.0.11` → `^7.2.0`. Zero Dart code changes required; `flutter analyze` clean.
- ✅ **Sequoia xattr patches committed** — see `SEQUOIA_XATTR_FIX.md`. Pod codesigning disable scoped to Debug/Profile (Release untouched to avoid App Store validation trap).
- ✅ **Provisioning API idempotency bug fixed and deployed** — root cause was plain `Error` objects without `.statusCode` mapping every Traccar 4xx to HTTP 500, plus no rollback on partial failure. Fix diagnosed by R2D2, deployed via `/tmp/apply-provisioning-fix.sh` at 19:29 UTC. Backup at `/opt/pettrack/provisioning-api/backups/20260422_192950/`. All 4 regression tests pass. Public endpoint confirmed returning 200 (idempotent) / 201 (fresh) / 409 (conflict) — no more 500s.

## Blocked / waiting

- ⚠️ **Apple Developer membership** — still in Apple-side validation. No action until they clear it; TestFlight workflow can't run end-to-end without it.
- ⚠️ **Firestore web init error** observed during Flutter web test (`cloud_firestore/unavailable` — client offline). Not yet diagnosed. Likely candidate: `firebase_options.dart` missing proper web config or the Firestore database was never created in the `pettrack-colombia` Firebase project.

## Pending (not today)

- 🔜 Rotate the GitHub PAT that was pasted in chat earlier.
- 🔜 Phase 3 — Flutter frontend alignment with the new HTTP status contract (201/200/409). Decide whether to surface distinct UX per status.
- 🔜 Android toolchain setup (optional — Flutter web is sufficient for daily iteration right now).

## State left behind on the backend

- Traccar has live records: `user id=15` (`nico.prueba@pettrack.co`) + `device id=6` (IMEI `862407061373209`) + link. Left intentionally so the next Flutter attempt hits the idempotent 200 path rather than provisioning fresh.
